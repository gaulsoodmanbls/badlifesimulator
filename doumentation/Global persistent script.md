## Global script


Bad Life Simulator uses one global script (/pages/scripts/global.gd) that persists across all parts of the game in order to keep track of variables that need to be able to be accessed or changed at any time. To access a global variable, type "global." before the variable name (e.g. "global.firstName = 'Brian'")
